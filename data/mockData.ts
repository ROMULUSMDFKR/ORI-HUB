import { api as firebaseApi } from '../api/firebaseApi';

// This file no longer contains mock data.
// It only re-exports the firebaseApi for any components that still import from here.
export const api = firebaseApi;
