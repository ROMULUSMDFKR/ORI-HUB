

import React from 'react';

const SettingsPage: React.FC = () => {
    // This page is no longer used for navigation.
    // The /settings route now redirects to the first settings page,
    // and the SecondarySidebar handles navigation.
    return null;
};

export default SettingsPage;